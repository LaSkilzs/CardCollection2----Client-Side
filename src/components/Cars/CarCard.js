import React from "react";
import CarDetails from "./CarDetails";

const CarCard = () => {
  return (
    <div>
      CarCard is Ready!
      <CarDetails />
    </div>
  );
};

export default CarCard;
